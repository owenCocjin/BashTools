#!/bin/bash
## Author:	Owen Cocjin
## Version:	1.3
## Date:	06/12/20
## Title:	cl
## Description: List directories after moving into them
## Notes:
##    - Fixed issue where cl doesn't go to home if no arguments were passed
##    - Fixed ls running even if bad arg passed

cl(){
	if [[ -z "$*" ]]; then
		cd ~
	else
		cd "$*"
	fi
	(($? == 0)) && ls --color=yes "$PWD"
}
